{% set name = "adsorption-file-parser" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/adsorption_file_parser-{{ version }}.tar.gz
  sha256: cd55a97f7b8f166d50ea0cba26359674289fd289838c4fb256d2215784dd708f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools_scm
  run:
    - python >=3.7
    - openpyxl
    - python-dateutil
    - xlrd >=1.1
    - xlwt >=1.3

test:
  imports:
    - adsorption_file_parser
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/AIF-development-team/adsorption-file-parser
  summary: Collection of parsers for (nearly) all commercial adsorption instrumentation.
  license: MIT
  license_family: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - pauliacomi
